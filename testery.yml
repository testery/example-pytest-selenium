test_selection:
  suites:
    - default:
      rules:
        - googleRule:
          when:
            - src_changes:
                - test/test_class.py
          then:
            - include_tags:
                - green
            - include_tags:
                - red
