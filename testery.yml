test_selection:
  suites:
    - default:
      rules:
        - googleRule:
          when:
            - src_changes:
                - test/test_basic.py
          then:
            - exclude_tags:
                - webtest
            - include_tags:
                - green